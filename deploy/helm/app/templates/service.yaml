apiVersion: v1
kind: Service
metadata:
  name: {{ include "rsoi-service.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "rsoi-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "rsoi-service.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
